apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.operatorgroup_grafanaboards.name  }}
  namespace: {{ .Values.namespace.grafanaboards  }}
spec:
  targetNamespaces:
  - {{ .Values.namespace.grafanaboards }}
